import * as fn from "./index.js"
import rootpath from "app-root-path"

// fn.createFolder(rootpath.resolve("/tmp/.foobar"), true) // inside sandbox
// fn.createFolder("/tmp/.foobar", false, true) // outside sandbox
// console.log(fn.absoluteSandboxPath("hey/foo/bar/baz/../../hello"))
